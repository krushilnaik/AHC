import React, { ReactElement } from 'react';

import '../scss/Footer.scss';

interface Props {}

function Footer({}: Props): ReactElement {
	return <footer>Acupuncture Herb Center © 2021</footer>;
}

export default Footer;
